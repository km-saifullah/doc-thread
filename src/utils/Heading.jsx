import React from "react";

const Heading = ({ text }) => {
  return (
    <h2 className="text-[44px] text-primary font-medium font-roboto leading-[140%]">
      {text}
    </h2>
  );
};

export default Heading;
